import { TextField } from "@mui/material"

import { Box } from "@mui/material";

const TitleInput = () => {
    return (
        <Box sx={{ backgroundColor: 'yellow', width: '100%', height: '100%' }}>
            fsadf
        </Box>
    )
}

export default TitleInput;